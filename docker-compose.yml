version: "3.5"

services:
  retail_db:
    image: postgres
    container_name: retail_pg
    ports:
      - "5452:5432"
    environment:
      - POSTRGRES_USER=postgres
      - POSTGRES_PASSWORD=Changeme
    volumes:
      - ./data/create_db_tables_pg.sql:/docker-entrypoint-initdb.d/schema.sql
    restart: always
    networks:
      - default
  dc-app:
    build: .
networks:
  default:
    name: data-copier-nw

  
